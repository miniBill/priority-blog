{
    "name": "elm-pages-app",
    "scripts": {
        "start": "elm-pages dev",
        "build": "elm-pages build",
        "build-debug": "elm-pages build --debug"
    },
    "devDependencies": {
        "elm-optimize-level-2": "^0.3.5",
        "elm-pages": "^3.0.19",
        "elm-review": "^2.12.0",
        "netlify-plugin-cache": "^1.0.3"
    },
    "overrides": {
        "elm-doc-preview": ">=6.0.0",
        "cross-spawn": ">=7.0.3"
    },
    "resolutions": {
        "elm-doc-preview": ">=6.0.0",
        "cross-spawn": ">=7.0.3"
    }
}
